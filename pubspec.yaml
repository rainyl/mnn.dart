name: mnn
description: A package for running MNN models on Dart and Flutter.
version: 0.0.1
repository: https://github.com/rainyl/mnn.dart

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  ffi: ^2.1.4
  logging: ^1.3.0
  meta: ^1.16.0
  native_assets_cli: ^0.11.0
  native_toolchain_cmake: ^0.0.4
  # path: ^1.8.0

dev_dependencies:
  ffigen: ^18.0.0
  image: ^4.5.4
  lints: ^5.0.0
  test: ^1.24.0

topics:
  - MNN
  - Machine Learning
  - Deep Learning
  - native-assets
